<h4 class="form_page_header"><%= "Edit #{@city.name}" %></h1>

<%= render partial: 'form' %>

<div class="form_wrapper">
<%= button_to "Delete the #{@city.name} page", "/cities/#{@city.id}",
  method: 'delete', class: 'waves-effect waves-light btn-large',
  data: {confirm: "Are you sure you want to delete this city?"} %>
</div>
